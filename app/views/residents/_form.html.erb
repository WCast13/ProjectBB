<%= form_for(resident) do |f| %>
  <% if resident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resident.errors.count, "error") %> prohibited this resident from being saved:</h2>

      <ul>
      <% resident.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resident-one %>
    <%= f.text_field :resident-one %>
  </div>

  <div class="field">
    <%= f.label :resident-twtwo %>
    <%= f.text_field :resident-twtwo %>
  </div>

  <div class="field">
    <%= f.label :lot_number %>
    <%= f.text_field :lot_number %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="field">
    <%= f.label :zipcode %>
    <%= f.text_field :zipcode %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
